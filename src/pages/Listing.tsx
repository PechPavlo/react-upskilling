import StackList from '../components/StackList/StackList';

const Listing = () => {
  return (
    <section>
      <h2>Stack Modules Page!</h2>
      <StackList />
    </section>
  );
};

export default Listing;
